# 모바일 확장 · 기기 간 동기화 설계 문서

작성일: 2026-09-02 · 대상 버전: v1.2.6 이후

PC 데스크톱 앱(Tauri)에 **모바일 앱**과 **기기 간 동기화**를 추가하기 위한 설계 문서.
아키텍처 결정의 근거, 이 앱 고유의 위험 요소, 단계별 로드맵을 담는다.

관련 문서: `CLAUDE.md`(프로젝트 규칙) · `figma-prd.md`(초기 기능 정의) · `src/lib/db.ts`(현행 스키마)

---

## 0. 제1원칙 — 기존 기능은 절대 깨지지 않는다

이 프로젝트의 최우선 제약이다. 한두 달간 실사용하며 잡아온 버그와 다듬은 UX가
동기화 작업 때문에 되돌아가는 일이 있어서는 안 된다. **아래 규칙은 모든 설계 결정에 우선한다.**

### 규칙 1 — 동기화는 기본 꺼짐(opt-in)

동기화를 켜지 않은 사용자의 코드 경로는 **현재와 100% 동일**해야 한다.
동기화 관련 코드는 전부 "켜져 있을 때만" 진입하는 분기 뒤에 둔다.
이것이 가장 강력한 안전장치다 — 최악의 경우에도 기능을 끄면 현재 상태로 돌아온다.

### 규칙 2 — 스키마 변경은 "순수 추가"만

- 기존 컬럼의 타입·이름·제약을 **변경하지 않는다**
- 기존 테이블을 **삭제하거나 구조를 바꾸지 않는다**
- 새 컬럼은 반드시 `DEFAULT`를 갖는다 → 기존 `INSERT`/`SELECT`가 그대로 동작
- 새 테이블 추가는 자유 (기존 쿼리에 영향 없음)

### 규칙 3 — 삭제는 tombstone **별도 테이블**로 (soft delete 아님)

**이 결정은 규칙 1·2를 지키기 위한 것이다.**

동기화에는 "무엇이 지워졌는지"를 알 방법이 필요하다. 일반적인 해법은
soft delete(`deleted_at` 컬럼 추가 + 실제 삭제 안 함)인데, 이 방식을 쓰면
**`api.ts`의 모든 `SELECT`에 `WHERE deleted_at IS NULL`을 추가해야 한다.**
현재 `api.ts`에는 90개의 export 함수가 있고, 한 곳이라도 빠뜨리면
**지운 항목이 화면에 되살아나는 버그**가 난다. 규칙 1을 정면으로 위반한다.

대신 이렇게 한다:

```sql
CREATE TABLE IF NOT EXISTS tombstones (
  entity     TEXT NOT NULL,   -- 'todos' | 'blocks' | 'notes' | ...
  entity_id  TEXT NOT NULL,
  deleted_at TEXT NOT NULL,
  device_id  TEXT NOT NULL,
  PRIMARY KEY (entity, entity_id)
);
```

- 기존 `DELETE FROM ...`은 **그대로 유지**한다
- 삭제 지점에 `INSERT INTO tombstones ...` **한 줄만 추가**한다
- 기존 `SELECT`는 **단 한 줄도 건드리지 않는다**

부수 효과로 삭제 성능과 화면 로직이 지금과 완전히 동일하게 유지된다.

### 규칙 4 — 리팩터링과 기능 추가를 같은 커밋에 섞지 않는다

App.tsx 분해, DnD 교체 같은 대규모 리팩터링은 **"동작 변화 0"** 커밋이어야 한다.
파일을 옮기고 나누기만 하고, 로직 수정은 반드시 별도 커밋으로 분리한다.
문제가 생겼을 때 어느 커밋이 원인인지 즉시 특정할 수 있어야 한다.

### 규칙 5 — 단계마다 실사용 검증 후 다음 단계

한 번에 전부 만들지 않는다. 각 단계를 릴리스하고 **본인이 최소 1주 실사용**한 뒤
다음 단계로 넘어간다. 로드맵(9장)이 이 단위로 쪼개져 있다.

### 규칙 6 — 마이그레이션 전 자동 백업

스키마 버전이 올라가는 모든 실행에서, 마이그레이션 **직전에** DB 스냅샷을 남긴다.
`src/lib/backup.ts`의 기존 백업 경로를 재사용한다.

### 규칙 7 — 회귀 확인 없이 릴리스하지 않는다

10장의 회귀 체크리스트를 릴리스 전 매번 수행한다.
자동 테스트가 아직 없는 영역은 수동으로라도 반드시 확인한다.

---

## 1. 목표와 비목표

### 목표

- PC와 모바일에서 **같은 데이터**를 보고 편집한다
- **서버 운영 비용 0원**을 유지한다
- 오프라인에서 완전히 동작한다 (현재 동작 유지)
- PC 앱의 **모든 기능**을 모바일에서도 사용할 수 있다

### 비목표 (이번 범위 아님)

- 실시간 공동 편집 (여러 사람이 같은 문서를 동시에)
- 웹 버전
- 소셜 기능(친구·리더보드) — 8장에 확장 경로만 정의
- 종단간 암호화(E2E) — 1차 출시 제외, 개인정보처리방침에 명시

---

## 2. 아키텍처 결정

### 2.1 저장 방식 — 로컬 SQLite 유지 + Google Drive appDataFolder 중계

```
   [PC 앱]                                    [모바일 앱]
  로컬 SQLite  ← 원본                        로컬 SQLite  ← 원본
      │                                           │
      │ 내 변경분만 업로드              내 변경분만 업로드 │
      ↓                                           ↓
      └────────→  Google Drive appDataFolder  ←───┘
                  (변경 로그 + 이미지)
                  = 중계소, 원본 아님
```

- **원본(source of truth)은 각 기기의 로컬 SQLite다.** 현재와 동일
- Drive는 **저장소가 아니라 중계소**다. DB 파일을 통째로 올리지 않는다
- 앱은 계속 로컬만 읽고 쓴다 → **속도·오프라인 동작이 현재와 동일**
- Drive가 죽어도, 인터넷이 끊겨도, 사용자가 연동을 해제해도 **앱은 정상 동작**한다

### 2.2 왜 Google Drive appDataFolder인가

| 항목 | 결과 |
|---|---|
| 저장 비용 | **0원** — 사용자 본인 Drive 할당량 사용 |
| 인증 비용 | **0원** — 구글 계정 |
| 서버 | **없음** — 앱이 Drive와 직접 통신 |
| OAuth 스코프 | `drive.appdata` = **비민감(non-sensitive)** → CASA 유료 보안감사 **불필요** |
| 폴더 성격 | 사용자에게 안 보임, 우리 앱만 접근, 공유 불가 |
| API 한도 | 프로젝트당 분당 100만 unit / 일 4억 unit → 적응형 폴링 시 **수만 명 규모** |

**핵심 근거**: `drive`, `drive.readonly` 같은 제한(restricted) 스코프는 매년 갱신하는
유료 CASA 보안 감사가 필요해 개인 개발자에게 사실상 불가능하다.
`drive.appdata`는 여기 해당하지 않는다.

### 2.3 검토했다가 기각한 대안

| 대안 | 기각 사유 |
|---|---|
| 사용자가 드라이브 폴더 수동 지정 | 설치 마찰, iOS 샌드박스, `(conflicted copy)` 파일로 데이터 분열 |
| LAN 동기화 | PC가 켜져 있고 같은 네트워크일 때만 동작 → 외출 시 무용 |
| Supabase (Postgres) | 무료 DB 500MB ≈ 누적 240 사용자·년, 다음 단계 월 $25 |
| Cloudflare (D1+R2+Workers) | 비용은 우수하나 인증·RLS를 직접 구현 → 보안 리스크와 개발 기간 |
| Firebase | 동기화 앱은 읽기 횟수로 무료 한도 소진, 관계형 스키마와 부적합 |

**Supabase / Cloudflare는 8장(소셜 기능)에서 재검토한다.** 그때는 성격이 다르다.

### 2.4 계정 없는 앱 + 선택적 드라이브 연동

**앱에 로그인 개념을 두지 않는다.**

```
앱 실행 → 즉시 사용 (계정 없음, 로컬 전용)
설정 → "기기 간 동기화 켜기" → 그때 구글 드라이브 연결
```

이유:

- 앱스토어 가이드라인 4.8(서드파티 로그인 시 Sign in with Apple 병행 요구)은
  **계정을 만드는 앱**에 적용된다. 계정이 없으면 적용 여지가 없다 → **iOS 출시 가능**
- 진입 장벽 0 — 로그인 강요 없음
- 현재의 로컬 전용 사용 방식이 그대로 **정식 지원 모드**가 된다 (규칙 1과 일치)
- 소셜 기능 도입 시점에 계정을 추가하면 되고, 그때도 **저장소는 Drive 그대로** 유지된다

사용자 식별자는 구글 OAuth의 `sub`(계정 고유 ID)를 그대로 쓴다.
나중에 소셜 서버를 붙일 때 마이그레이션 없이 **추가**만으로 연결된다.

---

## 3. 데이터 모델 변경

### 3.1 추가 컬럼 (동기화 대상 전 테이블)

규칙 2에 따라 **DEFAULT를 가진 추가 컬럼만** 넣는다.

| 컬럼 | 타입 | 용도 |
|---|---|---|
| `updated_at` | TEXT | 충돌 해결(LWW) 판정 기준. `notes`에만 있던 것을 전 테이블로 확장 |
| `updated_by` | TEXT | 마지막으로 수정한 기기 ID. 동률 시 결정론적 tie-break |

대상: `blocks`, `todos`, `deadlines`, `kanban_cards`, `notes`, `note_folders`,
`block_templates`, `checklist_items`, `todo_checklist_items`, `kanban_checklist_items`,
`timer_sessions`

### 3.2 timer_sessions 전용 추가

| 컬럼 | 타입 | 용도 |
|---|---|---|
| `device_id` | TEXT | **어느 기기의 세션인가.** 6.2 참조 — 없으면 심각한 버그 발생 |

### 3.3 신규 테이블

```sql
-- 삭제 기록 (규칙 3)
CREATE TABLE IF NOT EXISTS tombstones (
  entity     TEXT NOT NULL,
  entity_id  TEXT NOT NULL,
  deleted_at TEXT NOT NULL,
  device_id  TEXT NOT NULL,
  PRIMARY KEY (entity, entity_id)
);

-- 업로드 대기열 — 아직 Drive에 못 올린 변경분
CREATE TABLE IF NOT EXISTS sync_outbox (
  seq        INTEGER PRIMARY KEY AUTOINCREMENT,
  op_json    TEXT NOT NULL,
  created_at TEXT NOT NULL
);

-- 동기화 상태 (단일 행)
CREATE TABLE IF NOT EXISTS sync_state (
  id              INTEGER PRIMARY KEY CHECK (id = 1),
  device_id       TEXT NOT NULL,
  drive_cursor    TEXT,                          -- 마지막으로 읽은 Drive 변경 지점
  clock_offset_ms INTEGER NOT NULL DEFAULT 0,    -- 기기 시계와 구글 서버 시계의 차이
  last_sync_at    TEXT,
  last_error      TEXT
);
```

### 3.4 스키마 버전 관리 방식 변경

현재 `db.ts`는 `ALTER TABLE` 나열 + 개별 try/catch로 실패를 무시한다.
단일 기기에서는 문제없지만, 다중 기기 + 버전 스큐 환경에서는
**"이 DB가 어느 스키마 버전인가"를 명시적으로 알아야 한다.**

→ `PRAGMA user_version`으로 정수 버전을 도입한다.
기존 방어적 ALTER 로직은 **제거하지 않고 유지**한다(규칙 2). 기존 설치를 위한 안전망이다.

---

## 4. 동기화 프로토콜

### 4.1 Drive 파일 레이아웃

```
appDataFolder/
  manifest.json                    기기 목록, 각 기기의 진행 지점, 하트비트
  ops-{deviceId}-{seq}.jsonl       기기별 append-only 변경 로그
  snapshot-{deviceId}-{seq}.json   주기적 전체 스냅샷 (ops 압축용)
  img/{hash}.webp                  이미지
```

**핵심 성질: 각 기기는 자기 파일에만 쓴다.** 따라서 파일 단위 쓰기 충돌이 원천적으로 없다.

### 4.2 변경 로그(op) 포맷

```jsonc
{
  "v": 3,                             // 스키마 버전
  "ts": "2026-09-02T05:12:33.120Z",   // 보정된 수정 시각
  "dev": "device-uuid",
  "op": "set",                        // set | delete | bulk
  "entity": "todos",
  "id": "todo-uuid",
  "fields": { "title": "운동", "color": "#5AA9E6" }   // 바뀐 필드만
}
```

**필드 단위로 기록한다.** 행 전체를 덮으면, PC에서 제목을 고치고 폰에서 같은 항목의
색을 바꿨을 때 나중 것이 상대 변경을 지워버린다. 필드 단위면 둘 다 살아남는다.

### 4.3 충돌 해결

**같은 항목의 같은 필드**를 양쪽에서 고쳤을 때만 충돌이다.
서로 다른 항목이거나 다른 필드면 둘 다 적용된다 (실사용의 대부분).

판정 규칙:

1. **나중에 "고친" 것이 이긴다** — 나중에 "올라온" 것이 아니다.
   오프라인이라 늦게 업로드된 옛 수정이 최신 수정을 덮어쓰면 안 된다.
2. **시계 보정** — 기기 시계는 어긋날 수 있으므로, 동기화할 때마다 Drive 응답의
   구글 서버 시각과 비교해 `sync_state.clock_offset_ms`를 갱신하고 `ts` 기록 시 적용한다.
3. **동률이면 `updated_by`(기기 ID) 사전순** — 규칙이 반드시 **결정론적**이어야 한다.
   모든 기기가 독립적으로 계산해도 **같은 결론**에 도달해야 영구 불일치가 생기지 않는다.

특수 규칙:

- **삭제 vs 수정 → 삭제 우선.** 되살아나는 쪽이 사용자에게 더 놀랍다
- **순서 재배치(`sort_order`) → LWW로 한쪽 순서가 통째로 승리.**
  부분 병합된 뒤섞인 순서보다 "내가 마지막에 정한 순서"가 나오는 편이 낫다

### 4.4 두 종류의 시각을 혼동하지 말 것

| 시각 | 출처 | 용도 |
|---|---|---|
| **Drive 서버 시각** | 구글 (신뢰 가능) | 안 읽은 변경분 추적 — 동기화 커서 |
| **수정 시각 `ts`** | 보정된 기기 시계 | **누가 이기는지 판정** |

### 4.5 적응형 폴링

Drive는 서버 없이 푸시 알림을 줄 수 없다(webhook에 공개 HTTPS 주소 필요).
따라서 폴링하되 주기를 상황에 맞게 조절한다. **폴링 주기가 곧 API 할당량 소모다.**

| 상태 | 주기 |
|---|---|
| 앱 시작 / 창 포커스 획득 | **즉시 1회** ← 체감 품질의 90% |
| 활성 (창이 앞에 있음) | 30~60초 |
| `manifest.json` 하트비트로 **상대 기기도 활성**임을 확인 | **5초** |
| 최소화 / 백그라운드 | 5분 |
| 절전 · 화면 꺼짐 · 오프라인 | **중지** |

모바일은 백그라운드에서 폴링이 불가능하다(특히 iOS).
**"앱을 열 때 동기화"가 사실상 유일한 수신 경로**이며, 이는 문제가 되지 않는다 —
앱을 열지 않으면 데이터를 볼 일도 없다.

### 4.6 오프라인 동작

오프라인은 **특수 상태가 아니다.** 코드 경로가 동일하다.

```
수정 → 로컬 SQLite 기록 (즉시, 항상 성공)
     → sync_outbox 에 적재
     → 온라인이면 2~3초 디바운스 후 업로드
     → 오프라인이면 대기열에 머묾
     → 연결 복구 시 일괄 업로드 + 수신
```

모바일은 앱이 백그라운드로 전환될 때 **강제 flush**한다(iOS는 앱을 쉽게 종료시킨다).

### 4.7 압축(compaction)과 오래된 기기

ops가 쌓이면 주기적으로 스냅샷으로 접고 오래된 ops를 삭제한다.

> ⚠️ **반드시 구현할 것**: 자신의 커서가 압축 지점보다 오래된 기기는
> ops를 받을 수 없다. 이 경우 **스냅샷을 통째로 다시 받는 경로**가 필요하다.
> 없으면 오래 안 쓴 기기가 **조용히 어긋난 채로 동작**하는 최악의 버그가 된다.

---

## 5. 이미지 처리

- 이미지는 `img/{hash}.webp`로 개별 파일 업로드. DB에는 참조만 (현행 방식 유지)
- **업로드 전 강제 압축**: 장변 1600px, WebP q80.
  현재 평균 87KB → 40~50KB로 감소. 사용자 Drive 할당량과 API 호출을 아낀다
- 첫 동기화 시 이미지가 많으면 **Wi-Fi 대기 옵션** 제공 (셀룰러 요금 방지)

### ⚠️ 이미지 GC는 반드시 유예를 둔다

`src/lib/images.ts`의 `cleanupUnusedImages`는 "어떤 메모도 참조하지 않는 이미지를 삭제"한다.
로컬 전용에서는 완전히 옳지만, 다중 기기에서는 데이터를 파괴한다:

```
폰: 메모에 이미지 첨부 → 이미지 파일 + 메모 ops 업로드
PC: 이미지 파일은 받았으나 메모 ops는 아직 미처리
PC: "참조 없는 이미지네" → Drive에서 삭제
결과: 폰 메모의 이미지가 영구 소실
```

**대응**: `manifest.json`의 각 기기 진행 지점 중 **최솟값**을 기준으로 삼고,
그 이후 **30일 유예**를 지난 이미지만 삭제한다.

---

## 6. 이 앱 고유의 위험과 대응

일반적인 동기화 설계로는 잡히지 않는, 현재 코드 구조에서 비롯된 항목들이다.

### 6.1 반복 일정 — 결과가 아니라 "의도"를 보낸다 ⚠️ 최고 위험

현재 구현(`api.ts`)은 대량 연산이다:

```sql
UPDATE blocks SET ... WHERE repeat_group_id = ? AND date >= ?
DELETE FROM blocks WHERE repeat_group_id = ? AND date >= ?
```

이를 행 단위 op로 펼치면 두 가지 문제가 생긴다:

1. 사용자 동작 한 번이 op 수백 개가 된다
2. **기기마다 미리 생성해둔 반복 인스턴스의 범위가 다르다.**
   PC는 3개월치, 폰은 1개월치를 갖고 있다면 "이후 모두 삭제"의 결과가 기기마다 달라져
   **영원히 수렴하지 않는다**

**대응 — 의도(intent)를 하나의 op로 기록한다:**

```jsonc
{ "op": "bulk", "entity": "blocks", "action": "update",
  "where": { "repeat_group_id": "abc", "date_gte": "2026-09-10" },
  "fields": { "title": "운동" }, "ts": "..." }
```

받는 기기가 **자기 로컬 데이터에 같은 연산을 재실행**한다.
인스턴스 범위가 달라도 규칙이 같으므로 결과가 일관된다.

> 직관적 비유 — 워드의 "찾아 바꾸기":
> 결과 전송은 *"3번째 줄의 A를 B로, 7번째 줄의 A를 B로…"* (상대 문서의 줄 수가 다르면 엉망),
> 의도 전송은 *"문서 전체에서 A를 B로 바꿔라"* (문서가 달라도 일관되게 적용).

현재 SQL의 `WHERE` 조건을 그대로 op에 담으면 되므로 구조적으로 잘 맞는다.
`ts` 이후에 도착한 개별 수정은 살아남도록 적용 순서를 보장한다.

### 6.2 타이머 세션 — device_id 없이는 심각한 버그 ⚠️

**현재 버그가 될 코드**: `src/app/App.tsx:701` 부근이 앱 시작 시

```
fetchOngoingSessions()   ← 날짜 무관, 미마감 세션 "전부"
  → endTimerSession(s.id, "auto", s.lastAliveAt)   ← 전부 강제 마감
```

를 수행한다. 단일 기기에서는 올바르지만, 동기화가 붙으면
**폰을 켜는 순간 PC에서 실행 중인 타이머가 강제 종료된다.**

**대응:**

1. `timer_sessions.device_id` 추가
2. `last_alive_at` 기반 자동 마감은 **자기 기기 세션에만** 적용
3. 다른 기기의 진행 중 세션은 **읽기 전용 표시**

**동시 실행은 허용한다.** 오프라인에서는 상대 기기의 타이머 실행 여부를 알 수 없어
**막는 것이 애초에 불가능**하기 때문이다. 겹침 처리 로직은 어차피 필요하다.

**집중 시간은 합집합(union)으로 계산한다:**

```
PC :  14:00 ━━━━━━━━━━━ 15:00
폰 :         14:30 ━━━━━━━━━━━ 15:30

단순 합산 : 2시간   ❌ 부풀어남
합집합    : 1.5시간 ✅
```

한 사람이 물리적으로 두 곳에서 동시에 공부할 수 없으므로,
겹친 시간을 한 번만 세는 것이 **타협이 아니라 정확한 계산**이다.
`api.ts`의 `fetchFocusSecByDate`가 현재 세션별 지속시간을 단순 SUM하므로 이를 교체한다.

**UX**: 다른 기기에 진행 중 세션이 보이면 "PC에서 타이머 실행 중 (23분째)"를 표시하고
`[이어받기]` / `[따로 시작]` 선택지를 준다.

### 6.3 todo_category_orders — 행이 아니라 문서로 취급

`PRIMARY KEY (date, category)` 복합키에 `sort_order`만 있어 `id`가 없다.
tombstone/`updated_at`을 붙이기 애매하고, 성격상 "그 날짜의 순서 전체"를 통째로 덮어쓴다.

**대응**: `date`를 문서 id로 하는 **"순서 문서"** 하나로 취급해
`{ 카테고리: 순서 }` 맵을 통째로 LWW한다. id 부재 문제도 함께 해결된다.

### 6.4 중첩 구조의 고아 행

`checklist_items`, `todo_checklist_items`, `kanban_checklist_items`, `note_folders`는
`parent_id` 자기참조 + CASCADE 구조다. **CASCADE는 로컬 FK 동작일 뿐 동기화에는 통하지 않는다.**
A기기가 부모를 삭제하고 B기기가 그 자식에 항목을 추가하면 고아 행이 남는다.

**대응**: `api.ts`의 `deleteFolder`가 이미 하는 방식대로 —
삭제 시 **자손을 모두 모아 각각 tombstone을 남긴다.**
적용 후 고아 정리 패스를 안전망으로 한 번 더 돌린다.

### 6.5 버전 스큐 — 구버전이 신버전 데이터를 지운다 ⚠️

PC는 v1.4, 폰은 아직 v1.3인 상황. v1.4가 추가한 필드가 든 op를 v1.3이 받으면
모르는 필드이므로 무시하고, 그 항목을 수정해 다시 올릴 때 **그 필드 없이** 올린다.
**구버전 기기를 거치기만 해도 신기능 데이터가 조용히 사라진다.**

**대응:**

1. **모르는 필드는 건드리지 말고 보존해서 그대로 되돌려보낸다** (필수)
2. op에 스키마 버전(`v`)을 기록한다
3. **최소 지원 버전**을 두고, 그보다 낮은 버전은 동기화를 거부하고 업데이트를 유도한다

PC는 `src/lib/updater.ts`로 자동 업데이트되지만
**모바일은 사용자가 업데이트를 몇 달씩 미룰 수 있어** 실제로 발생하는 시나리오다.

### 6.6 동기화 버그는 되돌릴 수 없다 ⚠️

서버가 없다는 것은 "이상해졌으니 어제 상태로 복구해 주세요"가 **불가능**하다는 뜻이다.
동기화 엔진 버그 하나가 사용자 데이터를 영구 손상시킬 수 있다.

**대응 (둘 다 필수):**

1. **변경분 적용 직전 로컬 자동 스냅샷** — `backup.ts` 경로 재사용
2. **op 적용은 통째로 트랜잭션** — 중간 실패 시 전부 롤백

---

## 7. 모바일 앱

### 7.1 스택 — Capacitor

전 기능 이식이 목표이므로 **코드베이스를 두 벌 유지하는 선택지는 없다.**
React Native/Flutter는 UI 전체 재작성이라 기능이 계속 추가되는 이 앱에서는 곧 어긋난다.
Capacitor로 같은 React 코드를 쓰고 레이아웃만 화면 크기별로 분기한다.

로컬 DB는 `@capacitor-community/sqlite`. `tauri-plugin-sql`과 API가 다르므로
`db.ts`에 **얇은 드라이버 어댑터** 한 겹을 둔다.

### 7.2 선행 작업 (모바일 이전에 반드시)

| 작업 | 이유 | 데스크톱에도 이득 |
|---|---|---|
| **App.tsx 11,093줄 분해** | 도메인 로직·상태·데스크톱 레이아웃이 한 파일에 엉켜 있어 모바일 레이아웃을 분기시킬 접합점 자체가 없다 | ✅ 유지보수 |
| **HTML5 DnD → 포인터 이벤트 (37곳)** | `draggable`/`onDragStart`/`onDrop`은 **터치에서 전혀 동작하지 않는다.** 시간표 배치·할 일 재정렬·칸반·폴더 이동이 모두 여기 걸려 있다 | ✅ 안정성 |

> 참고: `package.json`의 `react-dnd`는 실제로 사용되지 않는 잔재다(Figma Make 유래).
> 실제 구현은 네이티브 HTML5 DnD다.

### 7.3 재설계가 필요한 화면

- **시간표 그리드**: 24시간 × 시작/종료 블록을 375px 폭에 넣는 것은 축소가 아니라
  다른 화면이다. 세로 타임라인 + 시간 선택 시트로 재설계
- **타이머 위젯**: 항상-위 창은 모바일에 대응물이 없다.
  안드로이드는 포그라운드 서비스 알림, iOS는 Live Activity

### 7.4 모바일 타이머의 난제

앱이 백그라운드로 가면 `touchTimerSession`(15초 주기)이 멈춘다.
다음 실행 시 `last_alive_at` 기준으로 마감되어 **백그라운드에 있던 시간이 통째로 사라진다.**
주머니에 넣고 공부한 시간이 0이 되는 문제다.

- **안드로이드**: 포그라운드 서비스로 프로세스를 살려 둔다
- **iOS**: `started_at` 기준으로 경과를 계산하되, 앱 복귀 시 "계속 진행 중이었나요?" 확인

---

## 8. 소셜 기능 (향후)

Drive만으로는 **불가능**하다. appDataFolder는 공유가 금지되어 있고,
사용자를 가로질러 계산할 장소가 존재하지 않는다.

**하이브리드로 해결한다** — 공유되어야 하는 데이터와 무거운 데이터는 성격이 다르다:

| | 데이터 | 위치 |
|---|---|---|
| 무겁고 사적 | 할 일, 메모, 타이머 세션 원본, 이미지 | **Drive** (0원) |
| 가볍고 공유 | 닉네임, 친구 관계, 하루 공부 시간, 스트릭 | **서버** |

실측 기준 1인당 연간: 전체 동기화 **2.1MB** vs 소셜 집계만 **약 15KB** — **140배 차이**.
Cloudflare 무료 티어(D1 500MB)로 **약 3만 사용자·년**을 감당한다.

**지금 해둘 준비는 하나뿐**: 구글 OAuth의 `sub`를 사용자 ID로 고정한다(2.4 참조).
그러면 소셜 서버 도입이 마이그레이션이 아니라 **순수 추가**가 된다.

**한계**: 공부 시간을 클라이언트가 자기 신고하므로 **치팅 검증이 불가능**하다.
서버 측 상식 검사(하루 24시간 초과 거부 등)로 완화하되, 상금이 걸린 랭킹에는 부적합하다.

---

## 9. 로드맵

각 단계는 **독립 릴리스**이며, 규칙 5에 따라 다음 단계 전 최소 1주 실사용 검증한다.

### 1단계 — 안전망 구축 (모바일 없이도 이득)

- [ ] Vitest 도입, `test` 스크립트 추가
- [ ] `api.ts` 핵심 함수 회귀 테스트
- [ ] 10.4 회귀 체크리스트를 실제 화면 기준으로 상세화
- [ ] 마이그레이션 전 자동 백업 (규칙 6)

### 2단계 — App.tsx 분해 (동작 변화 0)

- [ ] 도메인 로직을 `src/lib`으로 추출
- [ ] 화면을 컴포넌트 파일로 분리
- [ ] **로직 수정 없음** — 순수 이동만 (규칙 4)

### 3단계 — DnD 교체 (동작 변화 0)

- [ ] 포인터 이벤트 기반 DnD 유틸 작성
- [ ] 37개 지점을 하나씩 교체, 교체마다 데스크톱 동작 확인
- [ ] 미사용 `react-dnd` 의존성 제거

### 4단계 — 스키마 개편 (동기화 없이)

- [ ] `PRAGMA user_version` 도입
- [ ] `updated_at` / `updated_by` 추가 (전 테이블)
- [ ] `timer_sessions.device_id` 추가
- [ ] `tombstones` / `sync_outbox` / `sync_state` 테이블 추가
- [ ] 삭제 지점에 tombstone 기록 추가 (기존 SELECT 무수정)
- [ ] 집중 시간 계산을 **합집합** 방식으로 교체
- [ ] 자동 마감을 **자기 기기 세션 한정**으로 수정 (6.2)

### 5단계 — 동기화 엔진 (기본 꺼짐)

- [ ] 구글 OAuth (PKCE + 시스템 브라우저 + OS 키체인 저장)
- [ ] Drive appDataFolder 읽기/쓰기 계층 (`src/lib/sync.ts`로 격리)
- [ ] op 생성 / 적용 / 충돌 해결
- [ ] 반복 일정 intent op (6.1)
- [ ] 압축 + 오래된 기기 복구 경로 (4.7)
- [ ] 이미지 업로드 + 압축 + GC 유예 (5장)
- [ ] **결정론적 시뮬레이션 테스트** (10.2)
- [ ] 동기화 상태 UI (10.3)

### 6단계 — OAuth 검증 심사

- [ ] 개인정보처리방침 작성·게시, 홈페이지
- [ ] 구글 OAuth 앱 검증 신청 (**수 주 소요** — 미리 시작)
- [ ] 검증 전에는 테스트 사용자 100명 제한 + "확인되지 않은 앱" 경고

### 7단계 — 모바일 앱

- [ ] Capacitor 셸 + SQLite 드라이버 어댑터
- [ ] 모바일 레이아웃 분기
- [ ] 시간표 화면 재설계 (7.3)
- [ ] 모바일 타이머 (포그라운드 서비스 / Live Activity)
- [ ] 첫 동기화 Wi-Fi 대기 옵션

### 8단계 — 스토어 출시

- [ ] Play Store 데이터 안전 섹션
- [ ] App Store 심사
- [ ] 최소 지원 버전 정책 적용 (6.5)

### 이후 — 소셜 (8장)

---

## 10. 회귀 방지

### 10.1 자동 테스트 (1단계에 도입)

현재 테스트 파일과 `test` 스크립트가 **하나도 없다.** 지금까지는 직접 사용하며
버그를 잡는 방식으로 충분했으나, 동기화는 그 방식으로 잡히지 않는다 —
버그가 "두 기기가 특정 순서로 오프라인 편집했을 때만" 발생하고,
발견 시점에는 이미 데이터가 손상된 뒤이며 서버가 없어 복구도 불가능하다.

전체 커버리지는 목표가 아니다. **다음 두 영역만 확보한다:**

1. `api.ts` 핵심 CRUD — 리팩터링(2·3단계) 중 회귀 감지용
2. 동기화 엔진 — 아래 시뮬레이션

### 10.2 결정론적 시뮬레이션 테스트 (5단계)

로컬 퍼스트 동기화의 표준 검증법이다.

- 가상 기기 2~3개를 메모리에 생성
- 랜덤한 연산 시퀀스 + 랜덤한 네트워크 분단 주입
- 최종적으로 **모든 기기가 완전히 동일한 상태로 수렴하는지** 검증
- 시드를 고정해 실패를 재현 가능하게 한다

6장의 위험 항목들이 이 테스트로 대부분 잡힌다.

### 10.3 동기화 상태 UI (기능이자 지원 수단)

서버 로그가 없으므로 **사용자에게 상태를 보여주는 것이 곧 신뢰**다.
실제 지연보다 "상태가 보이는지"가 체감을 지배한다 —
표시가 없으면 2초도 "안 되는 것 아닌가" 싶고, 표시가 있으면 1분도 불안하지 않다.

- 마지막 동기화 시각 ("방금", "2분 전")
- 대기 중 변경 수 ("3개 대기 중")
- 오프라인 명시 ("오프라인 — 연결되면 자동 동기화")
- 수동 새로고침 버튼
- 마지막 오류 + **로그 내보내기** (사용자가 직접 제보할 수단)

**나중에 붙이는 것이 아니라 5단계에 포함한다.**

### 10.4 릴리스 전 수동 회귀 체크리스트

자동 테스트가 닿지 않는 영역을 매 릴리스 확인한다. (1단계에서 실제 화면 기준으로 상세화)

- [ ] 시간표: 블록 생성 / 드래그 배치 / 크기 조절 / 복사·붙여넣기 / 반복 설정
- [ ] 할 일: 추가 / 완료 / 재정렬 / 카테고리 순서("이 날짜만" · "이후 모두") / 반복
- [ ] 마감 + 칸반: 카드 이동 / 체크리스트 중첩
- [ ] 메모: 폴더 중첩 / 리치 텍스트·마크다운 모드 전환 / 이미지 붙여넣기·드롭 / 임시 저장 / Tab 들여쓰기
- [ ] 타이머: 시작·정지 / 뽀모도로 phase 전환 / 자정 롤오버 / 강제 종료 후 재실행 마감 / 별도 창 위젯
- [ ] 통계: 히트맵 / 날짜별 집중 시간
- [ ] 백업: 자동 백업 / JSON 내보내기·가져오기
- [ ] 업데이터: 자동 업데이트 확인

---

## 11. 미결정 · 향후 검토

- **종단간 암호화(E2E)** — appDataFolder는 구글이 기술적으로 접근 가능하다.
  기기 추가 시 키 전달 문제가 커서 1차 출시 제외. 개인정보처리방침에 명시하고
  향후 옵션으로 검토
- **계정 전환 정책** — 다른 구글 계정으로 연결 시 기존 로컬 데이터 처리
  (유력안: 계정별 로컬 DB 분리)
- **수익화 축** — Drive 방식은 서버 비용이 0이므로 "동기화 유료화"의 근거가 사라졌다.
  테마·통계 심화 등 다른 축으로 검토
- **구글 단일 의존 리스크** — 정책 변경 대비. 완화책은 동기화 계층을 `src/lib/sync.ts`
  뒤로 격리해 백엔드를 교체 가능하게 두는 것. Drive SDK든 다른 SDK든
  UI·도메인 코드에 노출하지 않는다 (`api.ts`가 `db.execute`를 감싼 것과 같은 방침)

---

## 부록 — 실측 데이터 (2026-09-02 기준)

설계 근거가 된 실제 사용 데이터. 45일 사용분.

| 항목 | 값 |
|---|---|
| DB 파일 크기 | 372KB (VACUUM 후 352KB) |
| todos | 315행 · 인덱스 포함 **행당 ~400B** · 하루 8.5개 |
| timer_sessions | 238행 · 행당 ~310B · 하루 6.4개 |
| notes | 7개 · 평균 2.6KB |
| blocks | 0행 (시간표 미사용) |
| 이미지 | 40장 / 3.39MB (평균 87KB) |

**환산:**

- 1인당 연간 텍스트 **약 2.1MB**, 이미지 **약 28MB** (이미지가 13배)
- 500MB = 할 일 125만 개 = 타이머 세션 160만 개(하루 6.4개면 690년치) = 메모 18만 개
- **개인 기준으로는 사실상 채울 수 없는 용량.** 사용자 Drive 15GB는 더욱 여유롭다
